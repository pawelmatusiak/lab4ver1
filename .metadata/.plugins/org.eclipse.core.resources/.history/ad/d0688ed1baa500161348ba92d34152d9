<html>
	<head>
		<title>Hello Facebook</title>
	</head>
	<body>
		<h3>Pawel Matusiak lab 4 obrazki z feed, <span th:text="${facebookProfile.name}">Some User</span>!</h3>
		
		<h4>Pictures:</h4>
		
		<div th:each="post:${feed}">
		<!--	<b th:text="${post.from.name}">from</b> wrote:
			<p th:text="${post.message}">message text</p> -->
			<p th:text="${post.birthday}">message text</p>
			<img th:if="${post.picture}" th:src="${post.picture}"/>
			<hr/>
		</div>
	</body>
</html>